<h2>Users: {{usersCount}}</h2>
<ul class="users-listing">
	{{#each user in controller}}
		<li>
			{{#link-to "user" user}}
				{{user.name}}
			{{/link-to}}
		</li>
	{{else}}
		<li>nobody</li>
	{{/each}}
</ul>
{{oulet}}
